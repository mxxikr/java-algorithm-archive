package Dp;

/**
 * ### 문제 설명
 *
 * '교대 패턴 수열'이란 [1, -1, 1, -1, ...] 또는 [-1, 1, -1, 1, ...]과 같이 1과 -1이 번갈아 나타나는 수열을 의미합니다.
 * 주어진 정수 수열의 '연속된 부분 수열'에 '교대 패턴 수열'을 원소별로 곱하여 새로운 수열을 만들 수 있습니다.
 *
 * 예를 들어, 수열 [5, -2, 3, -1]의 연속 부분 수열인 [-2, 3]에 교대 패턴 [1, -1]을 곱하면 [-2, -3]이 됩니다.
 * 같은 부분 수열 [-2, 3]에 다른 교대 패턴 [-1, 1]을 곱하면 [2, 3]을 얻을 수 있습니다.
 *
 * 정수 배열 `sequence`가 주어질 때, 만들 수 있는 모든 새로운 수열들 중에서 원소의 합이 가장 큰 값을 찾아 반환하는 함수를 작성하세요.
 *
 * #### 제한 사항
 *
 * * 1 <= `sequence`의 길이 <= 500,000
 * * -100,000 <= `sequence`의 각 원소 <= 100,000
 *
 * #### 입출력 예시
 *
 * | `sequence`                  | `result` |
 * |---------------------------|----------|
 * | `[2, 3, -6, 1, 3, -1, 2, 4]` | 10       |
 * | `[10, -4, 3, 1, 5, -3]`     | 24       |
 *
 * **예시 설명**
 *
 * **예시 #2**: `sequence`가 `[10, -4, 3, 1, 5, -3]`일 때,
 * 1. 교대 패턴 `[1, -1, 1, -1, 1, -1, ...]`을 `sequence` 전체에 곱하면 `[10, 4, 3, -1, 5, 3]`이 됩니다. 이 배열의 최대 연속 부분합은 `10+4+3-1+5+3 = 24` 입니다.
 * 2. 교대 패턴 `[-1, 1, -1, 1, -1, 1, ...]`을 `sequence` 전체에 곱하면 `[-10, -4, -3, 1, -5, -3]`이 됩니다. 이 배열의 최대 연속 부분합은 `1` 입니다.
 *
 * 두 경우 중 더 큰 값은 24이므로, 24를 반환합니다.
 *
 */
public class AlternatingPatternSum {
}
